# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: PyProgress Tracker

on:
  push:
    branches:
      - main  # Change this to your main branch name

jobs:
  build:
    runs-on: ubuntu-latest  # You can choose a different operating system if needed

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.12  # Specify the Python version you need

    - name: Set working directory
      run: cd $GITHUB_WORKSPACE

    - name: Grant execute permission to script
      run: chmod +x PyProgressTracker.py

    - name: Run Python script
      run: python PyProgressTracker.py
